// Name of logged in user (should come from authentication)
export const MYSELF = 'Myself';

// Max number of messages locally stored on the client
export const MAX_LOCAL_MESSAGES = 10000;

// Used for pagination / infinite scroll / virtual scroll
export const MESSAGES_PER_PAGE = 50;
